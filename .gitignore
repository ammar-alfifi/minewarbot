node_modules/
dist/
.env
*.log
.DS_Store
backend/data/
backend/.env
frontend/.env

# قفل الاعتماديات موحّد في جذر المشروع (npm workspaces)
backend/package-lock.json
frontend/package-lock.json

# بيانات اللعبة والنسخ الاحتياطية
backend/backups/
backend/data/*.db
backend/data/*.db-wal
backend/data/*.db-shm
.env

# Cloudflare / wrangler (حالة محلية ومخازن مؤقتة)
.wrangler/
.dev.vars

# حالة أدوات التحرير المحلية
# نستثني أوامر الفريق لتبقى مشتركة بين الأعضاء، ونُبقي بقية الحالة محلية
.opencode/*
!.opencode/commands/

