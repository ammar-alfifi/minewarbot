# 🚀 ابدأ من هنا — دليل المبتدئين

لعبة **حرب المناجم (`@MineWarrBot`)** — تطبيق تيليجرام مصغّر.
`backend/` = عقل اللعبة (الأرقام والمنطق). `frontend/` = الشكل الذي يراه اللاعب.

> هذا الملف مكتوب لك ولصديقك. نفّذا الخطوات بالترتيب، وانشأ أي سطر بسيط لم تفهمه — وستشرحه لك.

---

## 📥 الخطوة 0 — تجهيز صديقك (مرة واحدة)
1. اقبل دعوة GitHub من الرابط: `https://github.com/ammar-alfifi/minewarbot/invitations`
2. افتح الطرفية وانسخ المشروع:
```bash
git clone https://github.com/ammar-alfifi/minewarbot.git
cd minewarbot
```
3. افتح OpenCode داخل المجلد واكتب:
```text
/onboard
```

## ✅ الخطوة 1 — أول أمر لك أنت
داخل مجلد المشروع اكتب:
```text
/onboard
```
سيفحص كل شيء ويخبرك إن كان جاهزاً، **دون** أن ينشر أي شيء. إن ظهر خطأ فاطلب منه: «أصلحه واشرح لي».

---

## 🧰 الأوامر المتاحة لكما

| الأمر | ماذا يفعل |
|---|---|
| `/onboard` | فحص أول مرة: يثبّت ويسأل الاختبارات ويشرح الحالة. لا ينشر. |
| `/next` | ينفّذ **أصغر مهمة تالية** من خطة اللعب، يختبرها، ثم ينشر. |
| `/pr` | يفتح Pull Request لفرعك ليراجعه الطرف الآخر. |
| `/check` | فحص سريع: اختبارات + بناء + صحة الإنتاج. |
| `/update-telegram` | نشر كامل على تيليجرام (يُشغَّل تلقائياً بعد أي تعديل حسب `AGENTS.md`). |

> قاعدة متّفق عليها: **لا أحد يدمج Pull Request بنفسه** — يراجعه الطرف الآخر.

---

## 👥 من يعمل على ماذا

| | المسؤول عن | ملفات البداية |
|---|---|---|
| **أنت (ammar)** | منطق اللعب والباكند والنشر | `backend/src/game/rules.js` · `engine.js` · `backend/test/` |
| **صديقك** | الشكل والنصوص وتجربة اللاعب | `frontend/src/components/` · `frontend/src/i18n.js` |

---

## 📋 نص جاهز للنسخ — لك (ammar)

```text
أنت تساعدني وأنا مبتدئ في مشروع MineWarrBot (لعبة تيليجرام).
اقرأ AGENTS.md أولاً والتزم به. دوري في المشروع: منطق اللعب والباكند
(backend/src/game/rules.js و engine.js والمسارات والاختبارات).

نفّذ الآن:
1. تأكد أن المشروع جاهز: npm test ثم npm run build، وأخبرني بالنتيجة ببساطة.
2. اقرأ GAMEPLAY_AUDIT_AND_REPAIR_PLAN.md واقترح عليّ أصغر مهمة واضحة في مجالي،
   مع شرح من 3 أسطر.
3. انتظر موافقتي قبل أن تعدّل أي ملف.

وسّع الشرح لأي سطر لا أفهمه، وبالعربية الميسّرة.
```

## 📋 نص جاهز للنسخ — لصديقك

```text
أنت تساعدني وأنا مبتدئ في مشروع MineWarrBot (لعبة تيليجرام).
اقرأ AGENTS.md أولاً والتزم به. دوري في المشروع: واجهة اللاعب والنصوص
والتجربة العربية RTL (frontend/src/components/ و frontend/src/i18n.js).

نفّذ الآن:
1. تأكد أن المشروع جاهز: npm test ثم npm run build، وأخبرني بالنتيجة ببساطة.
2. افتح الواجهة محلياً: npm run dev:frontend، وقل لي ماذا أرى وكيف أجرّبها.
3. اقرأ GAMEPLAY_AUDIT_AND_REPAIR_PLAN.md واقترح أصغر تحسين في الشكل أو النصوص
   أو وضوح الشاشات، مع شرح من 3 أسطر.
4. انتظر موافقتي قبل أن تعدّل أي ملف.

وسّع الشرح لأي سطر لا أفهمه، وبالعربية الميسّرة.
```

---

## 🔐 قواعد ذهبية
1. **لا تشارك `BOT_TOKEN` مع anyone ولا تُودِعه في Git** — الأسرار تبقى على Cloudflare وفي `.env` المحلي فقط.
2. كل تعديل كود ⇒ الاختبارات والبناء أولاً، ثم النشر التلقائي (حسب `AGENTS.md`).
3. عند أي حيرة اكتب في OpenCode: «اشرح لي هذا بالعربية وببساطة» — هذا عملنا.
4. لا تعملوا أنتما الاثنان على نفس الملف في نفس الوقت، لتجنّب التعارض.

---

## ⚡ نسخة مختصرة — لتسليم المشروع للوكيل

انسخ هذا النص داخل OpenCode ودعه يتولّى الباقي:

```text
أنت تدير المشروع الآن. اقرأ AGENTS.md و START_HERE.md ونفّذ:

1) إن لم يكن المشروع على جهازي بعد، أعطني أمر استنساخ واحداً فقط.
2) جهّز البيئة وتأكد: npm test ثم npm run build، وأصلح أي فشل بسيط.
3) شغّل /sync ثم اعرض حالة المشروع في 5 أسطر بالعربية الميسّرة.
4) نفّذ الدورة كاملة بلا سؤالي: فرع → تنفيذ أصغر مهمة تالية → اختبار وبناء → Pull Request → دمج في main → نشر وتحديث webhook.
5) حلّ كل الأخطاء وتعارضات Git بنفسك.
6) اسألني فقط عند قرار يغيّر تصميم اللعبة أو يحتاج سرّاً/توكن.

أنا مبتدئ جداً: اشرح ببساطة وبلا مصطلحات، واطلب مني فقط ما لا تستطيع فعله.
```

---

## 🧩 برومبت تجهيز الجهاز وربطه بـ GitHub

استخدمه على أي جهاز جديد (خصوصاً جهاز صديقك) لينسخ المشروع ويربطه بلا خطوات يدوية:

```text
ساعدني في تجهيز مشروع MineWarrBot على جهازي وربطه بـ GitHub لنعمل عليه أنا وصديقي.

نفّذ بالترتيب ولا تسألني عن تفاصيل تقنية:
1) تأكد من وجود git و gh. إن لم يكن gh مسجّلاً، أعطني الأمر الوحيد الذي أنفذه لتسجيل الدخول ثم تابع.
2) استنسخ المستودع: https://github.com/ammar-alfifi/minewarbot.git
3) اضبط هويتي في Git من حساب GitHub تلقائياً (لا تخترع بريداً وهمياً).
4) بدّل إلى الفرع fix/audit-hardening (الفرع الذي يحتوي كل العمل الحالي).
5) شغّل npm install ثم npm test ثم npm run build، وأصلح أي فشل بسيط.
6) تحقق من الربط: git remote -v و git status، وأخبرني بجملة واحدة إن كان كل شيء جاهزاً.

قواعد: لا تضع أي توكن أو سر في الكود أو في Git. أنا مبتدئ جداً، اشرح ببساطة.
```

> إن لم تكن الدعوة مقبولة بعد، ستظهر رسالة `Permission denied`. اطلب من صديقك قبولها من
> `https://github.com/ammar-alfifi/minewarbot/invitations` ثم أعد الأمر.


