---
description: ابدأ فرع ميزة جديد من main محدّث
---

جهّز فرع ميزة جديد باسم مقترح من: $ARGUMENTS

نفّذ بالترتيب:
1. `git checkout main && git pull origin main`
2. أنشئ فرعاً باسم `feature/<اسم-مختصر-بالإنجليزي>` مشتقاً من main.

ثم أبلغني باسم الفرع فقط، وأظهر لي بأي ملف ستبدأ. لا تُنشئ أي commit بعد.
